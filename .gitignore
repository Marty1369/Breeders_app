# Dependencies & build output (app/.gitignore also covers these)
node_modules/
dist/
dist-ssr/

# Env / secrets — all env files ignored except the example. Supabase creds are
# no longer committed; src/lib/supabase.ts routes prod/staging by VITE_APP_ENV.
*.local
.env
.env.*
!.env.example

# Design working copies — regenerable from the handoff .zip
Design/extracted/
Design/extracted2/

# Logs
*.log
npm-debug.log*

# Claude Code per-user settings (keep launch.json shared)
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
*.suo
.idea/
